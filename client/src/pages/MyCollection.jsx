import React from "react";

const MyCollection = ({}) => {
  return (
    <div>
      <h1>My Collection</h1>
      <p>This is where we will keep the saved plants.</p>
    </div>
  );
};

export default MyCollection;
